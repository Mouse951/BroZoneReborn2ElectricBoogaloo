# Auto detect text files and perform LF normalization
* text=auto
Content/Assets/** filter=lfs diff=lfs merge=lfs -text
Content/Placeholders/** filter=lfs diff=lfs merge=lfs -text
Plugins/** filter=lfs diff=lfs merge=lfs -text

# Assign the 'Content folders here'